apiVersion: v1
kind: Service
metadata:
  name: ts-node-starter-service
  namespace: {{namespace}}
spec:
  type: NodePort
  selector:
    app: ts-node-starter
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP